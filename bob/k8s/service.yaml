apiVersion: v1
kind: Service
metadata:
  name: bob-orchestrator
  namespace: production
  labels:
    app: bob-orchestrator
spec:
  type: ClusterIP
  selector:
    app: bob-orchestrator
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    protocol: TCP

# Made with Bob
