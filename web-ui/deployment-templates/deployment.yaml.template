# Auto-generated Deployment by Bob
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{APP_NAME}}
  namespace: {{OPENSHIFT_NAMESPACE}}
  labels:
    app: {{APP_NAME}}
    generated-by: bob
spec:
  replicas: {{REPLICAS}}
  selector:
    matchLabels:
      app: {{APP_NAME}}
  template:
    metadata:
      labels:
        app: {{APP_NAME}}
        version: v1
    spec:
      containers:
      - name: {{APP_NAME}}
        image: docker.io/{{DOCKER_USERNAME}}/{{APP_NAME}}:latest
        imagePullPolicy: Always
        ports:
        - containerPort: {{APP_PORT}}
          protocol: TCP
        env:
        - name: PORT
          value: "{{APP_PORT}}"
        - name: ENVIRONMENT
          value: "{{OPENSHIFT_NAMESPACE}}"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: {{HEALTH_CHECK_PATH}}
            port: {{APP_PORT}}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: {{HEALTH_CHECK_PATH}}
            port: {{APP_PORT}}
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      imagePullSecrets:
      - name: dockerhub-secret
      restartPolicy: Always